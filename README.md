# rust-fundamentals-playground

A workspace for learning Rust from the ground up — real, compiling,
tested code instead of toy snippets.

Starting with a `fundamentals` crate covering the beginner topics, one
module per concept. More crates to follow as I work through the
language.

```sh
cargo test
```
